# 完成标准：{feature}

> 本文件说明“做到什么才算完成”。所有标为“必须通过”的验收项都已通过，或用户已明确确认跳过时，才能宣布完成。
> 本模板主要用于“完整检查”。轻量处理可直接在当前任务中记录范围与验收项，不必创建本文件。

## 完成标准状态

| 项 | 内容 |
| --- | --- |
| 改动名称 | 待填写 |
| 改动类型 | 常规改动 / 高风险改动 |
| 状态 | 草稿 / 已确认 / 修订中 |
| 确认时间 | - |
| 确认记录 | 待用户确认（记录用户原话或确认方式） |
| 完成标准检查 | 未检查 / 已通过 / 未通过 |
| 处理方式 | 完整检查 |
| 关联资料 | 需求、产品、UI、技术、测试、`specs/`、项目基本规则和代码规范 |

## ★ 目标与不做的事

### 目标

- 待填写（每条都应能由下方至少一个验收项证明）

### 这次明确不做

- 待填写

### 期望影响的业务指标（有相关资料时）

- 指标名称、预期方向和来源；没有基线时记录“暂无可用基线”，不编造数据。

## ★ 需要精确说明的词

| 词语 | 在本改动中的精确含义 | 不再使用的模糊说法 |
| --- | --- | --- |
| 待填写 | 例：“快”=冷启动到可交互 ≤ 2s（P95，指定设备） | 快、流畅 |

## 数据怎样流动、状态怎样变化

- 数据从哪里来、到哪里去、哪里是唯一权威来源。
- 关键对象有哪些状态、什么事件会触发变化、哪些变化必须拒绝。
- 任何时候都必须成立的约束。

## 出错时如何收场

| 出错场景 | 触发条件 | 系统行为 | 用户看到什么 | 怎样恢复 |
| --- | --- | --- | --- | --- |
| 待填写 | | | | |

> 至少检查弱网/超时、重复请求、并发冲突、中途退出、配额或成本耗尽、非法输入和依赖服务故障。不适用时写明理由。

## 质量上限与下限

| 方面 | 可检查的限制 | 怎样测量 |
| --- | --- | --- |
| 性能 | 例：关键接口 P95 ≤ 300ms | 待填写 |
| 资源 | 例：包体 ≤ 500KB / 内存 ≤ 200MB | 待填写 |
| 成本 | 例：单活跃用户月成本 ≤ $0.05 | 待填写 |
| 依赖 | 例：新增运行时依赖 ≤ 2 个 | 待填写 |
| 安全 | 例：无明文凭证；所有输入均校验 | 待填写 |

## 使用体验底线（有 UI 时）

- 首屏、进度反馈、空状态、错误状态、加载状态、可访问性和错误文案要求。
- 至少保留一个由人实际操作的体验验收项。

## 这次会改什么、不会改什么

- 允许修改：待填写（目录/模块/接口）。
- 禁止触碰：待填写（引用项目基本规则和代码规范）。
- 受影响的对外 API、数据结构或事件：待填写（对照 `specs/` 中的当前行为）。

## ★ 验收项

> 每项都必须绑定可重复的命令，或带步骤和明确判定标准的人工检查。代码改动后，受影响的旧结果必须重新验证。

| ID | 验收标准 | 怎样验证 | 方式 | 必须通过 | 结果 | 证据 | 更新时间 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A-001 | 例：当用户提交空表单时，系统应阻止提交并逐字段提示 | 例：`npm test -- form.spec` | 自动 | 是 | 未检查 | - | - |

## 完成标准检查

| # | 检查内容 | 结论 | 缺口与处理 |
| --- | --- | --- | --- |
| 1 | 数据来源、去向和状态变化 | 未检查 | |
| 2 | 出错时的处理和恢复 | 未检查 | |
| 3 | 质量限制 | 未检查 | |
| 4 | 验收方法可重复 | 未检查 | |
| 5 | 模糊词已精确说明 | 未检查 | |
| 6 | UI 体验底线 | 未检查 | |
| 7 | 业务指标关联 | 未检查 | |
| 8 | 改动范围 | 未检查 | |

## ★ 待确认项

> 确认完成标准前必须清零：逐条解决，或由用户确认为已记录假设。

- 待填写

## 已记录假设

- 待填写（附用户确认时间）

## 修订记录

| 时间 | 修订内容 | 原因 | 用户确认 |
| --- | --- | --- | --- |

<!--
内部兼容映射（默认不对用户展示）：
完成标准=完成合同；已确认=已冻结；完成标准检查=Definition Lint；验收项=Oracle；必须通过=blocking。
结果映射：未检查=NOT_RUN；已通过=PASS；未通过=FAIL；改动后需重查=STALE；已确认跳过=WAIVED。
旧复杂度代码：S=轻量改动，M=常规改动，L=高风险改动。
-->
