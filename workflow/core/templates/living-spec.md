# 行为规格：{领域或模块名}

> living spec：本文件描述**当前已实现并已发布**的行为真相，是 brownfield 需求的定义起点。
> 与 `features/` 的关系：features 是变更流水（做过什么），specs 是当前状态（现在是什么）。新需求先读本文件再定义变更；`/10-复盘总结` 在发布后把行为增量合并回本文件。
> 只记录已验证行为；未发布的计划不进本文件。

## 概览

- 该领域/模块解决什么问题、服务哪些场景（一段话）。
- 关联仓库与关键入口：待填写。

## 行为清单

> 用 EARS 句式（WHEN/WHILE/IF…THEN/WHERE + SHALL）逐条描述，稳定编号，不复用已删除编号。

| 编号 | 行为（EARS 句式） | 来源 | 生效版本 |
| --- | --- | --- | --- |
| S-001 | 例：WHEN 用户提交空表单 THE SYSTEM SHALL 阻止提交并逐字段提示 | features/xxx | v0.1.0 |

## 数据模型

- 关键实体、字段口径、唯一真相源、保留策略：待填写。

## 对外契约

- API / 事件 / 文件格式 / 快捷键等对外承诺，及其兼容性口径：待填写。

## 边界与已知限制

- 明确不支持的场景、已知缺陷及其 workaround：待填写。

## 变更历史

| 时间 | 变更 | 来源 feature | 影响的行为编号 |
| --- | --- | --- | --- |
