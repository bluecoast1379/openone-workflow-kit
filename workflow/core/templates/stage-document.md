# 阶段文档模板

## 结论

- 待填写

## 语言与展示

- 默认使用简体中文展示；专有名词、产品名、品牌名、代码标识符、命令、文件路径、分支名、API、SDK、框架、协议、标准、错误信息和官方英文术语保留原文。
- 不做无意义的中英双语重复；只有对外材料、多语言产品文案、合同/合规引用、用户明确要求翻译或目标市场需要时，才输出对应语言版本。

## 输入来源

| 来源 | 路径或证据 | 状态 |
| --- | --- | --- |
| team-profile | `workflow/team-profile.yaml` | 待核查 |
| 前序文档 | 工作区级 `features/{feature}/` | 待核查 |
| 本地代码 | 待填写 | 待核查 |
| 设计 / 规范 / 测试资料 | 待填写 | 待核查 |

## 事实与假设

### 已确认事实

- 待填写

### 待确认项

- 待填写

### 权限边界

- 个人项目中，agent 可以在范围明确且工作树已检查后执行本地分支命名、创建、commit、tag 和本地 merge。
- 未经用户明确授权，不执行远程 Git 刷新、推送、GitHub release、商店提交、部署、数据库写入或生产配置写入。
- 未通过实现准入，不修改业务代码、配置、脚本、迁移或部署文件。
- 不将内部工作流阶段产物、PRD、调研、法务、隐私、合规、复盘或内部决策材料写入目标代码仓库；公开 README、CHANGELOG、release notes、商店文案和隐私政策可以在发布阶段明确记录为代码侧材料。
- 不在 `main` 或 `prod` 上直接开发；实现默认在从 `prod` 创建的开发分支上进行，完成后合并到 `main` 测试，再由 `main` 提升到 `prod`；个人项目不创建 `test` 分支。
- 首个线上版本前不启用自动生产部署；CI/CD 变更必须记录触发分支、必跑检查、密钥存放、部署审批、回滚路径和证据。

## 输出

- 待填写
