schema_version: "1.0"
generated_at: "<ISO timestamp>"
generated_by: "agent-workflow-init"

team:
  name: "<TODO>"
  business_definition: "<TODO>"
  target_users: []

workspace_context:
  operating_defaults:
    planning_language: "zh-CN"
    display_language: "zh-CN-first"
    language_policy: "Use Simplified Chinese by default for workflow communication, stage deliverables, status summaries, reviews, tests, release records, and retrospectives; preserve official names and technical terms in their original form."
    terminology_policy: "Keep product names, brand names, code identifiers, commands, file paths, branch names, APIs, SDKs, frameworks, protocols, standards, error messages, and official English terms unchanged unless the user asks for localization."

enabled_tools: []
detected_tools: []

source_materials:
  business_intro:
    label: "业务介绍"
    status: "missing"
    paths: []
  project_docs:
    label: "项目资料"
    status: "missing"
    paths: []
  ui_specs:
    label: "UI 设计文件"
    status: "missing"
    paths: []
  frontend_rules:
    label: "前端开发规范"
    status: "missing"
    paths: []
  backend_rules:
    label: "后端开发规范"
    status: "missing"
    paths: []
  testing_rules:
    label: "测试规范"
    status: "missing"
    paths: []

repos: []

branch_model:
  type: "prod-main-development"
  production_branch: "prod"
  integration_branch: "main"
  development_branch_base: "prod"
  testing_branch: "main"
  test_branch_policy: "Do not create a separate test branch for personal projects; main is the testing/integration branch."
  release_flow: "Create development branches from current prod, develop locally on the development branch, merge completed work into main for testing, then promote main to prod for release."
  feature_branch_rule: "Use feature/<short-name>, fix/<short-name>, or dev/<short-name> development branches created from prod; agent may create local branches, local commits, local tags, and local merges after scope and working tree checks."
  worktree_dir: "_worktrees"

risk_policy:
  local_git_operations: "agent-allowed-after-scope-check"
  branch_creation: "agent-allowed-for-personal-repos"
  commit_tag_local_merge: "agent-allowed-after-verification"
  remote_git_operations: "explicit-user-authorization-required"
  push_and_release: "explicit-user-authorization-required"
  ci_workflow_changes: "manual-review-required"
  cd_workflow_changes: "manual-review-required"
  auto_deploy_before_first_prod_release: "blocked"
  production_deployments: "manual-approval-required-until-guarded-auto-approved"
  database_writes: "manual-only"
  config_writes: "manual-only"
  high_risk_files:
    - "ci/**"
    - "cd/**"
    - "deploy/**"
    - "*.env*"
    - "application*.yml"
    - "application*.yaml"
    - "bootstrap*.yml"
    - "bootstrap*.yaml"
    - "pom.xml"
    - "package.json"
    - "lockfiles"

workflow:
  features_dir: "features"
  core_dir: "workflow/core"
  adapters_dir: "workflow/adapters"
  require_stage_gate_for_code: true
  require_feature_branch_for_code: true
  same_repo_parallel_policy: "worktree-required-after-implementation-stage"
